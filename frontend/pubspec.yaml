name: musictab_frontend
description: Flutter 客戶端，提供使用者上傳音樂並查看轉譜結果。
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  flutter_hooks: ^0.20.5
  hooks_riverpod: ^2.5.1
  go_router: ^14.2.0
  dio: ^5.7.0
  supabase_flutter: ^2.7.1
  file_picker: ^8.0.3
  permission_handler: ^11.3.0
  intl: ^0.19.0

 dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
